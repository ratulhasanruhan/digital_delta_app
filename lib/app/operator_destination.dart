/// Primary navigation targets for the operator shell (M1–M8 + mesh).
enum OperatorDestination {
  dashboard,
  identity,
  supply,
  relay,
  map,
  pod,
  triage,
  modules,
  fleet,
  mesh,
  /// Hub grid: priorities, delivery, fleet, relay, flood risk, security.
  tools,
}
